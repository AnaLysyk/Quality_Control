# testes/ui/quality-control

Suite gerada automaticamente em 2026-07-10.

## Como rodar esta suite E2E (Playwright), todos os arquivos juntos

```powershell
npx playwright test testes/ui/quality-control
```

## Arquivos e casos de teste

### `qa-user-repository-reporting.spec.ts` (e2e (playwright))

- QA user validates the full quality-control flow in UI and confirms persistence via API

Rodar só este arquivo (isolado):
```powershell
npx playwright test testes/ui/quality-control/qa-user-repository-reporting.spec.ts
```
