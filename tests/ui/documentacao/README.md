# testes/ui/documentacao

Suite gerada automaticamente em 2026-07-10.

## Como rodar esta suite E2E (Playwright), todos os arquivos juntos

```powershell
npx playwright test testes/ui/documentacao
```

## Arquivos e casos de teste

### `documentacao-quality-control.spec.ts` (e2e (playwright))

- captures the main documentation screenshots when environment is configured

Rodar só este arquivo (isolado):
```powershell
npx playwright test testes/ui/documentacao/documentacao-quality-control.spec.ts
```
