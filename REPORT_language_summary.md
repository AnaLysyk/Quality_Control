# Language Summary — English vs Portuguese

**Date:** 2026-02-03

## High-level summary
- The repository is primarily Portuguese for UI, pages and many docs (examples: `app/empresas`, `app/defeitos`, README_AI_APPYING.md).
- English appears frequently in tests, test helpers, CI/debugging docs, TODO/FIXME comments and some backend utilities.
- Generated artifacts (e.g., `.next` server chunks) also contain large amounts of English (these are build outputs and should be ignored in language policy audits).

## Files / areas with notable English content (samples)
- `tests-e2e/` — many specs, helpers and fixtures use English (e.g., `helpers/mockAuth.ts`, `utils/seed-*.ts`, many `*.spec.ts`).
- `tests/` — unit tests and mocks (e.g., `tests/test_ai_applying_basic.py`, `tests/session/session.store.test.ts`).
- `ai_applying/backend_client.py` — runtime error/messages in English and technical terms.
- `app/api/kanban/export/route.ts` — route code and technical identifiers in English.
- Support/ops docs: `SUPPORT_TICKET_VERCEL_*.md`, `DEPLOYMENT_REPORT_*.md` — written in English.
- Misc: `PR_BODY.md`, `.github` workflow steps (CI log messages), many `TODO`/`FIXME` comments across the codebase.

## Notes about generated / third-party content
- Files under `.next/` and other build outputs contain English strings and TODOs from third-party code and build tooling — these are expected and should not be translated.
- External libraries and test frameworks (Playwright, Jest, respx, httpx, etc.) produce English-visible strings; keep those in English.

## Recommendations
- Keep UI/UX texts, README and product-facing docs in Portuguese if your audience is Portuguese-speaking.
- For developer-facing material (tests, CI logs, TODOs), decide a team convention: either continue using English (common in open-source/dev tooling) or migrate to Portuguese — be consistent.
- Remove committed build outputs (`.next`) from the repository and add them to `.gitignore` if not already ignored.
- Consider translating high-value README/guides to bilingual (Portuguese primary, English secondary) to help external contributors.
- If you want, I can:
  - produce a ranked file list with exact match counts, or
  - convert `TODO`/`FIXME` comments to Portuguese automatically (with a review pass).

## Quick next steps I can take (choose):
- Generate exact counts per file for English-keyword matches and output a ranked CSV.
- Create a simple script to transform `TODO`/`FIXME` comments to Portuguese (dry-run first).


---
Generated by assistant — repo scan for English keywords (common words and TODO/FIXME).