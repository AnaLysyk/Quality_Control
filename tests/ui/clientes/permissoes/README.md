# testes/ui/clientes/permissoes

Suite gerada automaticamente em 2026-07-10.

## Como rodar esta suite E2E (Playwright), todos os arquivos juntos

```powershell
npx playwright test testes/ui/clientes/permissoes
```

## Arquivos e casos de teste

### `clients-access.spec.ts` (e2e (playwright))

- admin global sees clients list
- user cannot access /admin/clients

Rodar só este arquivo (isolado):
```powershell
npx playwright test testes/ui/clientes/permissoes/clients-access.spec.ts
```
