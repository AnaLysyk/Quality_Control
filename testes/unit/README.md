# testes/unit

Suite gerada automaticamente em 2026-07-10.

## Como rodar esta suite (Jest), todos os arquivos juntos

```powershell
npx jest --config jest.config.ts testes/unit
```

## Arquivos e casos de teste

### `homeGreeting.test.ts` (unit/integracao (jest))

**Describe:** HomeContent helpers

- extracts first name or defaults to Ana

Rodar só este arquivo (isolado):
```powershell
npx jest --config jest.config.ts testes/unit/homeGreeting.test.ts
```
