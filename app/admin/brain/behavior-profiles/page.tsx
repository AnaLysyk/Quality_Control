import { BrainBehaviorProfiles } from "@/brain/_components/BrainBehaviorProfiles";

export const dynamic = "force-dynamic";

export const metadata = {
  title: "Perfis de comportamento do Brain",
};

export default function BrainBehaviorProfilesPage() {
  return <BrainBehaviorProfiles />;
}
