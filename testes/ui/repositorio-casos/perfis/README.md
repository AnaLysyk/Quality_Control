# testes/ui/repositorio-casos/perfis

Suite gerada automaticamente em 2026-07-10.

## Como rodar esta suite E2E (Playwright), todos os arquivos juntos

```powershell
npx playwright test testes/ui/repositorio-casos/perfis
```

## Arquivos e casos de teste

### `case-repository-profile-cycle.spec.ts` (e2e (playwright))

**Describe:** Repositório Central - ciclo completo por perfil

- @case=TC-PROFILE-CYCLE-${profile.key} ${profile.label} cria caso, plano e run no próprio contexto

Rodar só este arquivo (isolado):
```powershell
npx playwright test testes/ui/repositorio-casos/perfis/case-repository-profile-cycle.spec.ts
```
