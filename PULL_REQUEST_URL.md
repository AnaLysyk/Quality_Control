Pull Request disponível

A branch `i18n/translate-to-pt` foi enviada e o PR pode ser criado abrindo o link abaixo no navegador:

https://github.com/AnaLysyk/Quality_Control/pull/new/i18n/translate-to-pt

O corpo do PR está em `PR_BODY.md` no repositório.

Se preferir criar o PR via CLI local, rode este comando no seu ambiente:

```bash
gh pr create \
  --title "i18n: traduzir docs e mensagens para PT-BR" \
  --body-file PR_BODY.md \
  --base main \
  --head i18n/translate-to-pt \
  --label i18n \
  --label chore
```

Observação: o `gh` CLI não está disponível neste ambiente, por isso não pude criá-lo automaticamente.
